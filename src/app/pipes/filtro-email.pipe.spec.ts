import { FiltroEmailPipe } from './filtro-email.pipe';

describe('FiltroEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
